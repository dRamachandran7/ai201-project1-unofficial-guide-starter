sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1
requests==2.32.5

# Query interface — add whichever you use for Milestone 5:
gradio==4.44.1  # newest gradio compatible with Python 3.9 (gradio 5+ needs 3.10+)
# streamlit>=1.40.0

# Document ingestion — add if your documents/ folder contains .pdf files:
# pdfplumber==0.11.4
